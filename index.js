/**
 * @format
 */

import { AppRegistry } from 'react-native';
import Jolenjokeeapp from './Jolenjokeeapp';
import { name as appName } from './app.json';

AppRegistry.registerComponent(appName, () => Jolenjokeeapp);
